# 🤖 Jarvis — Arduino AI Assistant

Голосовой ИИ-ассистент с анимацией рта на LCD-дисплее 16x2.

- **Arduino Uno** управляет LCD I2C-дисплеем и датчиком расстояния HC-SR04
- **Python на ПК** слушает микрофон, общается с OpenAI GPT, озвучивает ответ
- Дисплей **оживает** когда ты подходишь, и **засыпает** когда уходишь

---

## Как это выглядит

```
Ожидание:                 Слушает:                 Говорит:
┌────────────────┐        ┌────────────────┐        ┌────────────────┐
│  [ JARVIS ]    │        │  [ LISTENING ] │        │  [ SPEAKING ]  │
│       ──       │        │      ◉◉        │        │      ()        │
└────────────────┘        └────────────────┘        └────────────────┘

Сон:
┌────────────────┐
│     zzZ...     │
│      ~_~       │
└────────────────┘
```

---

## Схема работы

```
[Человек подходит] → HC-SR04 будит систему
       ↓
[LCD]: строка 0 — "LISTENING", строка 1 — пульсирующий рот
       ↓
[Микрофон]: распознавание речи (Google, русский)
       ↓
[OpenAI GPT]: генерация ответа
       ↓
[LCD]: строка 0 — "SPEAKING", строка 1 — анимация рта
[Динамик]: озвучка ответа
       ↓
[Человек уходит 10 сек] → засыпает, экран показывает zzZ
```

---

## Железо

| Компонент | Описание |
|-----------|----------|
| Arduino Uno | Основная плата |
| LCD 16x2 + I2C модуль (PCF8574) | Дисплей с анимацией рта |
| HC-SR04 | Датчик расстояния — автопробуждение |
| Микрофон | Подключён к ПК |
| Динамик / наушники | Подключены к ПК |
| USB кабель | Arduino ↔ ПК |

---

## Схема подключения

### LCD I2C → Arduino Uno

```
LCD I2C    Arduino Uno
───────    ───────────
VCC    →   5V
GND    →   GND
SDA    →   A4
SCL    →   A5
```

### HC-SR04 → Arduino Uno

```
HC-SR04    Arduino Uno
───────    ───────────
VCC    →   5V
GND    →   GND
TRIG   →   D7
ECHO   →   D6
```

### Визуальная схема

```
                  Arduino Uno
               ┌─────────────────┐
               │   A4  ← SDA     │◄── LCD I2C
               │   A5  ← SCL     │◄── LCD I2C
               │                 │
               │   D7  → TRIG    │──► HC-SR04
               │   D6  ← ECHO    │◄── HC-SR04
               │                 │
               │   5V ───────────┼──► LCD VCC, HC-SR04 VCC
               │   GND ──────────┼──► LCD GND, HC-SR04 GND
               │      USB        │
               └────────┬────────┘
                        │
                   ┌────┴────┐
                   │   ПК    │
                   │ Python  │
                   │  🎤 🔊  │
                   └─────────┘
```

---

## Установка

### 1. Arduino

1. Открой **Arduino IDE**
2. Установи библиотеку **LiquidCrystal I2C**:
   `Sketch → Include Library → Manage Libraries → "LiquidCrystal I2C"`
   (автор: Frank de Brabander или johnrickman)
3. Открой `arduino/jarvis_mouth.ino`
4. Выбери `Tools → Board → Arduino Uno`
5. Выбери нужный порт (`Tools → Port`)
6. Нажми **Upload**

> **Если дисплей не включается** — у твоего модуля может быть другой I2C-адрес.
> Запусти [I2C Scanner](#определение-i2c-адреса) и смени адрес в коде:
> ```cpp
> LiquidCrystal_I2C lcd(0x3F, 16, 2); // вместо 0x27
> ```

### 2. Python (ПК)

**Требования:** Python 3.8+

```bash
# Клонировать репозиторий
git clone https://github.com/Tanjiro00/Jarvis_arduino.git
cd Jarvis_arduino/python

# Зависимости для голосового режима
# Linux:
sudo apt install portaudio19-dev espeak espeak-data

pip install openai SpeechRecognition pyaudio pyttsx3 pyserial
```

**Получить API-ключ OpenAI:** https://platform.openai.com/api-keys

---

## Запуск

### Текстовый режим (тест без микрофона / на сервере)

```bash
cd python
export OPENAI_API_KEY="sk-proj-..."   # Linux / Mac
set OPENAI_API_KEY=sk-proj-...        # Windows CMD

python main.py
```

Пишешь текст — Джарвис отвечает. `выход` для завершения.

### Голосовой режим (ПК с микрофоном + Arduino)

```bash
# Arduino определится автоматически
python main.py --voice

# Или указать порт вручную
python main.py --voice COM3            # Windows
python main.py --voice /dev/ttyUSB0   # Linux
```

---

## Анимации дисплея

Команды Python → Arduino по Serial:

| Команда | Действие |
|---------|----------|
| `M0` | Рот закрыт — горизонтальная линия |
| `M1` | Слегка приоткрыт |
| `M2` | Средне открыт |
| `M3` | Широко открыт |
| `M4` | Максимально открыт (с зубами) |
| `L1` | Режим "слушаю" — пульсирующая анимация |
| `A1` | Запустить автоанимацию разговора |
| `A0` | Остановить анимацию, вернуть idle |
| `S1` | Режим сна (zzZ) |
| `S0` | Выход из сна |

Arduino → Python по Serial:

| Сообщение | Событие |
|-----------|---------|
| `WAKE` | Человек подошёл (< 100 см) |
| `SLEEP` | Человек ушёл (> 10 сек) |

---

## Структура проекта

```
Jarvis_arduino/
├── arduino/
│   └── jarvis_mouth.ino    # Скетч Arduino (LCD I2C + HC-SR04)
├── python/
│   ├── main.py             # Главный скрипт (текстовый и голосовой режим)
│   ├── ai.py               # OpenAI GPT (gpt-4o-mini)
│   ├── speech.py           # Распознавание речи (Google Speech API)
│   ├── tts.py              # Синтез речи (pyttsx3)
│   ├── serial_comm.py      # Serial-связь с Arduino
│   └── requirements.txt    # Зависимости
└── schemas/
    └── wiring.md           # Подробная схема подключения
```

---

## Настройки

### Расстояние активации

В `arduino/jarvis_mouth.ino`:
```cpp
const int WAKE_DISTANCE_CM = 100; // порог в сантиметрах
```

### Задержка перед сном

```cpp
const unsigned long SLEEP_DELAY = 10000; // 10 секунд
```

### Скорость анимации рта

```cpp
const unsigned long TALK_FRAME_MS   = 130; // мс между кадрами разговора
const unsigned long LISTEN_FRAME_MS = 350; // мс между кадрами "слушаю"
```

### Модель GPT

В `python/ai.py`:
```python
model="gpt-4o-mini"  # быстро и дёшево
model="gpt-4o"       # умнее, дороже
```

### Характер Джарвиса

В `python/ai.py`, переменная `SYSTEM_PROMPT`:
```python
SYSTEM_PROMPT = "Ты — Джарвис, умный и лаконичный ИИ-ассистент. ..."
```

---

## Определение I2C-адреса

Если дисплей не реагирует — загрузи этот скетч, открой Serial Monitor (9600):

```cpp
#include <Wire.h>
void setup() {
  Wire.begin();
  Serial.begin(9600);
  for (byte addr = 1; addr < 127; addr++) {
    Wire.beginTransmission(addr);
    if (Wire.endTransmission() == 0) {
      Serial.print("Found I2C: 0x");
      Serial.println(addr, HEX);
    }
  }
}
void loop() {}
```

Найденный адрес подставь в `jarvis_mouth.ino`:
```cpp
LiquidCrystal_I2C lcd(0x3F, 16, 2); // ← замени адрес
```

---

## Частые проблемы

**`portaudio.h: No such file or directory`** при установке pyaudio:
```bash
sudo apt install portaudio19-dev   # Ubuntu/Debian
brew install portaudio             # Mac
```

**Arduino не найдена автоматически:**
```bash
python main.py --voice /dev/ttyUSB0   # Linux
python main.py --voice COM3           # Windows
```

**Нет русского голоса в pyttsx3 на Linux:**
```bash
sudo apt install espeak espeak-data
```

**Дисплей включился, но ничего не видно:**
- Покрути синий потенциометр на I2C-модуле сзади — это регулировка контрастности

---

## Требования

- Python 3.8+
- Arduino IDE 1.8+ или 2.x
- OpenAI API ключ
- Интернет (для Google Speech Recognition и OpenAI API)
